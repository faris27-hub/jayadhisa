<?php require_once 'functions.php'; if(user()) redirect('index.php'); $title='Login';$error=''; if($_SERVER['REQUEST_METHOD']==='POST'){check_csrf();$email=trim($_POST['email']??'');$pass=$_POST['password']??'';$s=$koneksi->prepare('SELECT * FROM users WHERE email=? LIMIT 1');$s->bind_param('s',$email);$s->execute();$u=$s->get_result()->fetch_assoc();if($u&&password_verify($pass,$u['password'])){session_regenerate_id(true);$_SESSION['user']=$u;redirect($u['role']==='admin'?'admin/index.php':'index.php');}$error='Email atau password salah.';} include 'header.php'; ?>
<div class="auth"><div class="panel"><span class="pill">AKUN</span><h1>Login</h1><?php if($error):?><div class="alert error"><?=e($error)?></div><?php endif;?><form method="post"><input type="hidden" name="csrf" value="<?=csrf()?>"><label>Email</label><input type="email" name="email" required autocomplete="email"><label>Password</label><input type="password" name="password" required autocomplete="current-password"><button class="btn full">Login</button></form><p>Belum punya akun? <a href="register.php">Daftar sekarang</a></p><div class="demo"><b>Admin demo</b><br>admin@jayadhisa.test<br>admin123</div></div></div><?php include 'footer.php'; ?>
